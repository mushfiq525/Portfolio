# Dependencies
node_modules

# Next.js build output
.next
out
build
next-env.d.ts
*.tsbuildinfo

# Environment
.env*
!.env.example

# Hosting
.vercel

# Editor and local tooling
.claude
.vscode
.idea

# Auto-generated by create-next-app; local agent guidance, not project source
AGENTS.md
CLAUDE.md

# The résumé the hero's button links to. Deliberately NOT committed: the CV at
# D:\DOCS\Mushfiqur Rahman (Research) CV.pdf lists the two referees' email
# addresses, and this repo (plus the deployed site) is public — the same reason
# those addresses are left off the References section. To enable the download,
# export a copy of the CV with the referee emails removed, save it here as
# public/resume.pdf, then delete this rule and commit the file.
public/resume.pdf

# OS and logs
.DS_Store
Thumbs.db
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
